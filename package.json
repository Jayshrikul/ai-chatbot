{
  "name": "ai-chatbot-full",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd ai-chatbot-full && npm install && npm run build",
    "start": "node server/index.js"
  },
  "dependencies": {
    "express": "^4.19.0",
    "@google/generative-ai": "^0.10.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5"
  }
}
